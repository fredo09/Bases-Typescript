"use strict";
(() => {
    /**Usando Tuplas**/
    /**
     *
     * Definicion de las Tuplas
     * Const array : [string, number] = [<Ndatos>];
     * Esto puede llevar varos tipos de valores como definicion del array
     */
    const hero = ['Spiderman', 100];
    hero[0] = 'Batman';
    hero[1] = 200;
    console.log(hero);
})();
